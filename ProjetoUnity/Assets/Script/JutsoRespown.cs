using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JutsoRespown : GameController
{
    // Start is called before the first frame update
    void Start()
    {
        InstanciarJutso();

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
